# 改善計画書 (IMPROVEMENT_PLAN.md)

## 概要

このドキュメントは、swingプロジェクトの継続的な改善のためのロードマップです。プロジェクトの品質、パフォーマンス、保守性を向上させるための具体的な計画と実装状況を記録しています。

## 改善の優先順位

### 🔴 優先度1: セキュリティとデータ整合性

#### 1.1 認証情報管理の改善
- **現状**: 認証情報（メール/パスワード）が平文でJSONファイルに保存
- **目標**: 環境変数またはセキュアなシークレット管理システムの導入
- **実装計画**:
  - [ ] 環境変数からの認証情報読み込み機能の実装
  - [ ] 既存のJSONファイルからの移行ツールの作成
  - [ ] パスワードハッシュのみ保存する仕組みの導入
- **進捗**: 未着手

#### 1.2 データベースマイグレーション
- **現状**: マイグレーションスクリプトのテストが不足
- **目標**: データ破損を防ぐ堅牢なマイグレーションシステム
- **実装計画**:
  - [x] 包括的なインデックス作成スクリプトの実装（`db/create_comprehensive_indexes.py`）
  - [ ] マイグレーションのロールバック機能の実装
  - [ ] マイグレーションテストの追加
- **進捗**: 30% 完了

### 🟠 優先度2: ビジネスクリティカルな機能の品質向上

#### 2.1 テストカバレッジの向上
- **現状**: 55%（2025年7月9日時点）
- **目標**: 80%以上
- **実装済み**:
  - [x] ポートフォリオ可視化のテスト作成（78%カバレッジ達成）
  - [x] スクリーニング機能のテスト拡充（78%カバレッジ達成）
- **残タスク**:
  - [ ] Web UIのテスト追加（現在53%）
  - [ ] データ取得モジュール（fetch/）のテスト追加
  - [ ] バックテストモジュールのテスト拡充
- **進捗**: 55% → 目標80%

#### 2.2 TODO.mdの実装
- **現状**: 現物取引の処理に不具合あり
- **目標**: 現物取引を含むすべての取引タイプの正確な処理
- **実装計画**:
  - [ ] 現物取引の損益表示修正
  - [ ] パフォーマンス集計への現物取引合算
  - [ ] 取引履歴CSVの現物取引表示修正
- **進捗**: 未着手

### 🟡 優先度3: パフォーマンス最適化

#### 3.1 pandas操作の最適化
- **実装済み**:
  - [x] `iterrows()`のベクトル化（4ファイルで完了）
    - `src/portfolio/visualization.py`
    - `backtest/backtest_ml.py`
    - `backtest/backtest_technical.py`
    - `backtest/analyze_backtest_json.py`
- **効果**: 大量データ処理時の処理速度向上
- **進捗**: 100% 完了

#### 3.2 データベースクエリ最適化
- **実装済み**:
  - [x] 包括的なインデックス作成スクリプト
  - [x] WALモードの有効化
  - [x] クエリ分析機能の実装
- **効果**: クエリ実行速度の向上、並行アクセス性能の改善
- **進捗**: 100% 完了

### 🟢 優先度4: 開発効率向上

#### 4.1 コード品質の統一
- **実装済み**:
  - [x] エラーハンドリングの標準化
    - 共通例外クラス（`src/utils/exceptions.py`）の作成
    - エラーハンドリングデコレータの実装
  - [x] ログ出力フォーマットの統一
    - 構造化ログフォーマッターの実装
    - JSON形式のログ出力サポート
  - [x] 型ヒントの完全実装
    - 主要モジュールへの型ヒント追加
- **進捗**: 100% 完了

#### 4.2 ドキュメント整備
- **実装済み**:
  - [x] IMPROVEMENT_PLAN.md（本ドキュメント）の作成
- **残タスク**:
  - [ ] APIリファレンスの自動生成（Sphinx）
  - [ ] アーキテクチャ設計書の作成
  - [ ] 開発者向けガイドの充実
- **進捗**: 30% 完了

## 実装済み改善の詳細

### パフォーマンス最適化

#### pandas操作のベクトル化
```python
# Before
for _, row in df.iterrows():
    process(row)

# After
df.apply(process, axis=1)
# または
vectorized_process(df)
```

#### データベースインデックス
以下のインデックスを追加：
- `prices`テーブル: `(code, date)`, `(date)`, `(code)`
- `statements`テーブル: `(code, DisclosedAt)`, `(code)`, `(DisclosedAt)`
- `holdings`テーブル: `(user_id, code)`, `(user_id)`, `(deleted_at)`

### コード品質

#### 標準化されたエラーハンドリング
```python
from src.utils.exceptions import handle_exceptions, DatabaseError

@handle_exceptions(logger, default_return=None)
def risky_operation():
    # エラーが発生する可能性のある処理
    pass
```

#### 構造化ログ
```python
from src.utils.logging_config import get_logger

logger = get_logger("module.name")
logger.info("処理完了", extra={"user_id": 123, "items": 50})
```

## 今後の改善計画

### 短期（1-2週間）
1. セキュリティ強化
   - 環境変数による認証情報管理
   - APIトークンの暗号化

2. テストカバレッジ向上
   - Web UIのテスト追加
   - 統合テストの整備

### 中期（1-2ヶ月）
1. アーキテクチャの改善
   - リポジトリパターンの導入
   - 依存性注入の実装

2. CI/CDの強化
   - 自動デプロイメントパイプライン
   - パフォーマンステストの自動化

### 長期（3-6ヶ月）
1. マイクロサービス化の検討
   - データ取得サービスの分離
   - スクリーニングサービスの独立化

2. 機械学習モデルの高度化
   - より複雑な特徴量エンジニアリング
   - リアルタイム予測システム

## 進捗管理

改善作業を行う際は：
1. このドキュメントの該当セクションを更新
2. 実装完了時は [x] でチェック
3. 進捗率を更新
4. 実装の詳細を「実装済み改善の詳細」セクションに記録

最終更新日: 2025年7月9日
